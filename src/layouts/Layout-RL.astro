---
import BaseLayout from "./BaseLayout.astro";
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<BaseLayout title={title}>
  <main>
    <div class="wrapper">
      <div class="text">
        <p class="large-text semi-bold">
          El <span class="large-text semi-bold">diagnóstico temprano</span>
           reduce la complejidad del sufrir y vivir la enfermedad
        </p>
        <div class="items">
          <div class="item">
            <div class="number button-text">1</div>
            <p class="small-text">
              Te brindamos asesorías mediante información según el malestar que
              tengas.
            </p>
          </div>

          <div class="item">
            <div class="number button-text">2</div>
            <p class="small-text">
              Te facilitamos el contacto de personal médico y centros de salud
              según diagnósticos.
            </p>
          </div>

          <div class="item">
            <div class="number button-text">3</div>
            <p class="small-text">
              Puedes centralizar tu información médica en un solo lugar
            </p>
          </div>
        </div>
      </div>

      <div class="form">
        <slot />
      </div>
    </div>
  </main>
</BaseLayout>

<style>
  main {
    background-color: var(--secondary-50);
    padding: 3rem 1rem;
  }

  .wrapper {
    display: flex;
    width: 70rem;
    justify-content: center;
    align-items: flex-start;
    gap: 4rem;
  }

  .text {
    display: flex;
    max-width: 30rem;
    flex-direction: column;
    align-items: flex-start;
    gap: 2rem;
  }

  .text span {
    color: var(--secondary-500);
  }

  .text .items {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
  }

  .items .item {
    display: flex;
    justify-content: center;
    align-items: flex-start;
    gap: 1.5rem;
  }

  .item .number {
    display: flex;
    width: 2rem;
    height: 2rem;
    justify-content: center;
    align-items: center;
    border-radius: 2rem;
    color: var(--light);
    background: var(--secondary-400, #487cc9);
  }

  .item p.small-text {
    max-width: 15rem;
  }

  .form {
    display: flex;
    min-width: 28rem;
    padding: 2rem;
    flex-direction: column;
    align-items: flex-start;
    gap: 2rem;
    border-radius: 1rem;
    background: var(--light, #f4f4f4);
  }
</style>
