---
import Divider from "./svgs/Divider.astro";
import DividerTop from "./svgs/DividerTop.astro";
---

<div class="hero">
  <div class="wrapper">
    <div class="text">
      <h2>El cáncer de piel se puede diagnosticar a tiempo</h2>
      <p>
        En <b>HealthMedia</b> te acompañamos con prevención y orientación para
        este y otros tipos de males.
      </p>

      <a href="/" class="btn">Quiero saber más</a>
    </div>
    <img
      src="/images/hero-doctors.png"
      alt="Doctores sonrientes"
      height="360px"
    />
  </div>
</div>

<style>
  .hero {
    position: relative;
    background-color: var(--primary-100);
    /* padding-top: 32px; */
    padding: 2.5rem 0px;
    margin-bottom: 32px;
    z-index: 10;
  }

  .img-onda {
    height: 100px;
    width: 100%;
  }

  .hero .wrapper {
    display: flex;
    flex-direction: row;
    gap: 48px;

    justify-content: center;
    align-items: center;
  }

  .wrapper .text {
    display: flex;
    flex-direction: column;
    gap: 24px;
    color: var(--dark);
    max-width: 25rem;
  }

  .text h2 {
    color: var(--secondary-500);
    font-weight: 600;
    text-align: left;
  }

  .text .btn {
    background-color: var(--secondary-600);
    color: var(--light);
    font-weight: 600;

    width: fit-content;
    transition-property: color, background-color;
    transition-duration: 0.3s;
  }

  .text .btn:hover {
    background-color: var(--secondary-500);
  }

  /** RESPONSIVE **/
  @media only screen and (max-width: 768px) {
    .hero .wrapper img {
      height: 160px;
    }
    .text h2 {
      font-size: 30px;
    }
  }

  @media only screen and (max-width: 650px) {
    .hero .wrapper {
      flex-direction: column;
    }
  }
</style>
